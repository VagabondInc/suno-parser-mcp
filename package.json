{
  "name": "suno-parser-server",
  "version": "1.0.0",
  "description": "MCP server for parsing Suno.com song pages using Playwright",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "ts-node server.ts",
    "build": "tsc",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.46.0",
    "fastify": "^4.26.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}